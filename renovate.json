{
  "extends": [
    "config:base"
  ],
  "regexManagers": [
    {
      "fileMatch": ["^Dockerfile.php$"],
      "matchStrings": ["FROM (?<depName>.*?):[0-9,.]*-(cli-drupal|cli|fpm)-v(?<currentValue>.*)$"],
      "datasourceTemplate": "docker"
    }
  ]
}
